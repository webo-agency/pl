{
  "name": "@webo-agency/www-pl",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "analyze": "nuxt build --analyze",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "deploy": "npm run generate"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "nuxt": "^3.7.3"
  }
}
